# Personal Website

Portfolio and blog built with Vite + React.

## Requirements

- **Node.js** 22 (recommended; use `nvm use 22` if you use nvm)
- **npm** (comes with Node.js)

## Run locally

```bash
nvm use 22
npm install
npm run dev
```
